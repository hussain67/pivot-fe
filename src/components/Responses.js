const Responses = () => {
    // presentation_id and slide_id 
    const noOfRes = 0;
    return (
        <div>
            <h5>Responses<span>{` ( ${noOfRes} ) `}</span></h5>
        </div>
    )
}

export default Responses;